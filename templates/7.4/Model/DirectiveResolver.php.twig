<?php

declare (strict_types=1);
{% if namespace|length > 0 %}
namespace {{ namespace }};
{% endif %}

use Axtiva\FlexibleGraphql\Generator\Exception\NotImplementedResolver;
use GraphQL\Type\Definition\ResolveInfo;
use Axtiva\FlexibleGraphql\Resolver\DirectiveResolverInterface;
{% for import_class in import_classes %}
use {{ import_class }};
{% endfor %}

/**
 * This code is @generated by axtiva/flexible-graphql-php
 * Resolver for executable directive @{{ directive_name }}
{% if directive_description|length > 0 %}
 * {{ directive_description }}
{% endif %}
 */
final class {{ short_class_name }} implements DirectiveResolverInterface
{
    /**
     * @param callable $next
     * @param {% if directive_args_class %}{{ directive_args_class }} {% endif %}$directiveArgs
     * @param $rootValue
     * @param $args
     * @param $context
     * @param ResolveInfo $info
     * @return mixed
     */
    public function __invoke(callable $next, $directiveArgs, $rootValue, $args, $context, ResolveInfo $info)
    {
        throw new NotImplementedResolver('Not implemented directive resolver ' . __CLASS__);
        // FIXME example return mb_strtoupper($next($rootValue, $args, $context, $info));
    }
}